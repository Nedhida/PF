<div class="yield-layout">

  <div class="values-show">
    <h2 class="values-show__title">固定費データ詳細</h2>

    <%= render 'kakeibo-p/links' %>
    <div class="values-show__content">
      <div class="values-show__list">
        <div class="values-show__box">
          <div class="values-show__head">日付</div>
          <div class="values-show__obj">
            <%= @fixedcost_value.start_time.strftime('%Y/%m/%d')%>
          </div>
        </div>
        <%= render 'fixedcost_values/show', item: @fixedcost_value %>
      </div>
      <div class="move-page">
        <%= link_to '編集する', edit_fixedcost_value_path(@fixedcost_value), class:"move-page__edit" %>
      </div>
      <div class="move-page">
        <%= link_to '削除する', fixedcost_value_path(@fixedcost_value), method: :delete, data: { confirm: '本当に消しますか？' }, class:"move-page__delete" %>
      </div>
    </div>
    <div class="move-page">
      <%=link_to '１日の登録', day_records_path(start_time: @fixedcost_value.start_time.strftime('%Y/%m/%d')) %>
    </div>

  </div>
</div>