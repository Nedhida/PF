<div class="yield-layout">

  <div class="value-new">
    <h2 class="value-new">支出登録フォーム</h2>
    <%= render 'kakeibo-p/links' %>
    <%= form_with model:@variablecost_value, url: variablecost_values_path, method: :post, local: true do |f| %>
      <%= render 'form', variablecosts: @variablecosts, f: f %>
    <% end %>
  </div>

</div>