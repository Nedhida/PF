<div class="incomes-edit">
  <div class="incomes-edit__title">収入項目編集</div>
  <div class="incomes-edit__register">
    <%= form_with model: @income, url:income_path, local: true do |f| %>
      <%= render 'form', f: f %>
      <%= f.submit %>
    <% end %>
  </div>
</div>